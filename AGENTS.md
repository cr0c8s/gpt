# Agents

## Cursor Cloud specific instructions

This repository is currently an empty/blank project with only a `README.md`. There are no applications, services, dependencies, build steps, lint checks, or tests to run.

- **No package manager or lockfile** is present — no `package.json`, `requirements.txt`, `go.mod`, etc.
- **No services** need to be started.
- **No environment variables** are required.

When code is added to this repository, update this section with relevant development setup instructions, service startup caveats, and testing guidance.
